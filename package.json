{
  "name": "cyberia-website",
  "version": "1.0.0",
  "description": "WIP for Cyberia website",
  "main": "index.js",
  "scripts": {
    "build": "cd server && npm install && cd ../client && npm install && npm run build", 
    "start": "node --env-file=config.env server/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
